/** Keep in sync with repo root `src/lib/faceScanCaptures.ts`. */
export const FACE_SCAN_CAPTURE_STEPS = [
  { id: "front", title: "Front face" },
] as const;

export const FACE_SCAN_INSTRUCTIONS = [
  "Take one clear front-facing photo — good light, face only, eyes open.",
] as const;
